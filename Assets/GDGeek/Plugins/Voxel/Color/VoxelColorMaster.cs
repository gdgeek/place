using UnityEngine;
//using UnityEditor;
using System.Collections;
using System.Collections.Generic;


namespace GDGeek{
	/*
	public class VoxelColorMaster : MonoBehaviour {

		public List<VoxelColorSet> _list; 

		public void refresh ()
		{

			GDGeek.VoxelMesh[] meshs = GameObject.FindObjectsOfType<GDGeek.VoxelMesh> ();
			if (_list != null) {
				_list.Clear ();
			} else {
				_list = new List<VoxelColorSet>();			
			}
			for (int i = 0; i< meshs.Length; ++i) {

				VoxelColorSet set = new VoxelColorSet();
				set.setup(meshs[i]);
				Debug.Log (meshs[i].name);	
				_list.Add(set);
			}

		}

		void Awake(){
			Debug.Log ("asdfsaf");
		}



	}*/
}