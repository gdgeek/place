using UnityEngine;
using System.Collections;
using System;


namespace GDGeek{
	/*
	public class VoxelFunctionManager : MonoBehaviour {


		[Serializable]
		public class Item{
			public int id = 0;
			public VoxelFunction func;
		};
		
		[SerializeField]
		public Item[] _itemList = null;


		public void build (VoxelMesh mesh)
		{
			foreach(VoxelHandler handler in mesh._datas){
				foreach (Item item in _itemList) {
					if(item.id == handler.id){
						item.func.build(mesh, handler);
					}			
				}
			}
		}

	}*/
}